# Complexity-Project
This Project measures the time complexity of a data set (list of random integers) using two different methods. 
Method 1 uses brute force (nested for loops) and has a time complexity of O(n^2).
Method 2 uses a smarter approach and has a time complexity of O(n).
You can change the variable iterations to specify the number of doubling that occurs to the list.